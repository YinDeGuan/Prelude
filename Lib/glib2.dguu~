//

#include "type.h"
#include "const.h"     ;const use macro that defined in protect.h
#include "protect.h"
#include "proto.h"
#include "proc.h"
#include "global.h"


PUBLIC void delay(int time)
{
  int i = 0 , j = 0 , k = 0 ;

  for( k = 0 ; k < time ; k ++ )
    for(i = 0 ; i < 10 ; i++)
      for(j = 0 ; j < 10000 ; j ++ ) ;

  
           
}


//1647D916
//negiup
